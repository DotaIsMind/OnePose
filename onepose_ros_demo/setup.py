# This file is intentionally empty.
# Python package installation is handled entirely by
# ament_python_install_package() in CMakeLists.txt.
# Keeping a setup() call here caused duplicate CMake custom targets
# (CMP0002 conflict) because ament_cmake_python processes setup.py
# a second time on top of the explicit ament_python_install_package() call.
